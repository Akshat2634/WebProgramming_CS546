{{!
This page will be rendered when the following route is hit: GET http://localhost:3000/movie/:id
The title of the document should be the name of the Movie. You should have the title set as the <title> element of
    the HTML document.

    Your page should reference a CSS file, /public/css/styles.css; his file should have at least 10 rulesets you must
    use at
    least 5 of them on this page.


    }}

<article class="movie-details">
  <a href="/">Search for Another Movie</a>
  <h1 class="movie-title">{{movie.Title}}</h1>
  <img src="{{movie.Poster}}" alt="{{movie.Title}} Poster" />
  <h2>Plot</h2>
  <p>{{movie.Plot}}</p>
  <section>
    <h3>Info</h3>
    <dl>
      <dt class="info-title">Year Released:</dt>
      <dd class="info-data">{{movie.Year}}</dd>
      <dt class="info-title">Rated:</dt>
      <dd class="info-data">{{movie.Rated}}</dd>
      <dt class="info-title">Runtime:</dt>
      <dd class="info-data">{{movie.Runtime}}</dd>
      <dt class="info-title">Genre(s):</dt>
      <dd class="info-data">{{movie.Genre}}</dd>
      <dt class="info-title">Box Office Earnings:</dt>
      <dd class="info-data">{{movie.BoxOffice}}</dd>
      <dt class="info-title">DVD Release Date:</dt>
      <dd class="info-data">{{movie.DVD}}</dd>
    </dl>
  </section>
  <section>
    <h4>Cast and Crew</h4>
    <p><strong>Director:</strong> {{movie.Director}}</p>
    <p><strong>Writer:</strong> {{movie.Writer}}</p>
    <p><strong>Cast:</strong> {{movie.Actors}}</p>
  </section>
  <section>
    <h4>Ratings</h4>
    <table class="my_coolratings_table">
      {{#each movie.Ratings}}
        <tr>
          <td>{{this.Source}}</td>
          <td>{{this.Value}}</td>
        </tr>
      {{/each}}
    </table>
  </section>
</article>